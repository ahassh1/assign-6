#### 1) What is the difference between `null` and `undefined`?

##### Null is basically having no value, and undefined is valiable is declared but it value is not initialized or assined

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?

##### map is an object that can store key value pair and loop another array and return new array, on the otherhand foreach is also loop another array but it is not return new array

#### 3) What is the difference between `==` and `===`?

##### double equal is compare value and triple equal is campare value and type both

#### 4) What is the significance of `async`/`await` in fetching API data?

##### in js code run in asyncronusly but async/await with fetch to handle https request that's very clean code and run synchronously

#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).

##### Global scope like var that is delare any position of the body and function scope that's variable works inside the funcitoin and block scope like if else state ment inside the curly braces statement

### ( সহজ সরল সিম্পল ) ASSIGNMENT (SwiftCart E-Commerce)

---

🛍️ API Endpoints

---

1. Get 🛍️ All Products

```bash
https://fakestoreapi.com/products
```

2. Get 🛍️ All Categories

```bash
https://fakestoreapi.com/products/categories
```

3. Get 🛍️ Products by Category

```bash
https://fakestoreapi.com/products/category/${category}
```

Example:

```bash
https://fakestoreapi.com/products/category/jewelery
```

4. Get 🛍️ Single Product Detail

```bash
https://fakestoreapi.com/products/${id}
```

Example:

```bash
https://fakestoreapi.com/products/1
```

---

```https://sweet-beignet-b073cd.netlify.app/product

```
